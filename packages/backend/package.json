{
  "name": "@x402place/backend",
  "version": "0.1.0",
  "private": true,
  "main": "./src/server.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "build": "esbuild src/server.ts --bundle --platform=node --outfile=dist/server.js --external:@prisma/client --external:.prisma/client"
  },
  "dependencies": {
    "@x402place/shared": "workspace:*",
    "hono": "^4.6.14",
    "@hono/node-server": "^1.13.7",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/ws": "^8.5.13",
    "esbuild": "^0.24.0",
    "tsx": "^4.7.0",
    "typescript": "^5.6.3"
  }
}

